## 1. Shared Foundation (do first — every tab group below depends on this)

- [x] 1.1 Extend `apps/api/prisma/schema.prisma`: add `description`, `stage`, `origin`, `escalated`, `contact`, `referralId`, `openedDate`, `closedAt`, `nextHmisReviewDue`, `hmisDataQualityStatus`, `followUpMilestone`, `followUpDueDate`, `createdById`, `updatedById` to `Case` (all nullable or defaulted per design.md Decision 1; `referralId`/`createdById`/`updatedById` as nullable FKs added once `Referral`/`User` relations exist). Extend `InteractionSummary` with `interactionPurpose`, `confidentialityType`, `partnerAccount`, `offering`, `relatedRecordType`, `relatedRecordId`.
- [x] 1.2 Add the `Task` model (design.md Decision 3) with FKs to `User` (owner), `Client`, and nullable `Case`/`GoalAssignment`/`InteractionSummary`.
- [x] 1.3 Run `prisma migrate dev` for this first migration; verify `apps/api` still builds and existing endpoints (`GET /api/cases`, `GET /api/cases/:id`) still respond.
- [x] 1.4 Add `apps/web/src/components/ui/Modal/` (design.md Decision 12) with a story in `ui-preview/stories/`.
- [x] 1.5 Add `apps/web/src/components/ui/ExpandableRow/` (design.md Decision 12) with a story in `ui-preview/stories/`.
- [x] 1.6 Add `apps/api/src/models/task.model.ts`, `task.mapper.ts`, `src/services/task.service.ts`, `src/controllers/task.controller.ts`, `src/routes/task.routes.ts`: `GET /api/cases/:id/tasks`, `POST /api/tasks`, `PATCH /api/tasks/:id`, `PATCH /api/tasks/:id/status`. Mount in `routes/index.ts`.
- [x] 1.7 Add `Task`/`CaseUpdateInput`/`CaseCreateInput`/`CaseDetail` field additions to `packages/types/src/cases.ts` and a new `packages/types/src/tasks.ts`.
- [x] 1.8 Extend `apps/api/src/utils/logger.ts`'s redact paths for any new PII-bearing fields introduced this change (task `description`, interaction summary `partnerAccount`/`offering` if sensitive — confirm against existing redact list).

## 2. Case Operations Center — New Case, Header, Edit, KPI Trend

- [x] 2.1 Extend `case.model.ts`/`case.mapper.ts`/`case.service.ts` to require `subject` on create, default `openedDate`, set `createdById` from `req.user`, and compute the Active Cases month-over-month trend for `GET /api/cases`'s existing `kpis` payload.
- [x] 2.2 Extend `PATCH /api/cases/:id` in `case.controller.ts`/`case.service.ts` to accept `description`/`stage`/`origin`/`escalated`/`contact`/`referralId`/`hmisDataQualityStatus`/`nextHmisReviewDue`, setting `updatedById`.
- [x] 2.3 Add `PATCH /api/cases/:id/follow-up` (`case.routes.ts`/`case.controller.ts`/`case.service.ts`) implementing design.md Decision 2's `addDays(now(), milestone)` computation and `none` clearing both columns.
- [x] 2.4 Add `apps/api/src/constants/caseStageOptions.ts` / `caseOriginOptions.ts` (or fold into `hudOptions.ts`-style constants) for the Stage/Origin HUD-coded-scalar option lists; serve via `/api/reference` alongside existing HUD options.
- [x] 2.5 Frontend: build the New Case modal (`apps/web/src/features/cases/newCase/NewCaseModal.tsx`) using `Modal`, a client-search field, and `Button`; wire `createCase` thunk in `casesSlice`; show the two specified toasts.
- [x] 2.6 Frontend: wire the New Case button into `CasesPage.tsx`'s existing table-card header row (matching My Clients' "New Intake" placement convention).
- [x] 2.7 Frontend: implement the Active Cases KPI tile's trend line in `CasesPage.tsx`'s KPI row.
- [x] 2.8 Frontend: build the full case detail header in `CaseDetailPage.tsx` (title, case number, client link, status/referral/priority/stage/origin) and the Edit form/modal; wire `updateCase` thunk; preserve list filter/search on Back (confirm existing `cases-screen` Back behavior already does this or extend it).
- [x] 2.9 Verify: create a case via the modal with and without Subject (409/400 vs success per spec); edit a case's Stage/Origin/HMIS fields and confirm they persist; confirm Back returns to the list with filters intact. **Verified at the API level against a real fixture client** (see Group 3.8's note): `POST /api/cases` with no subject correctly 400s "Subject is required to create a case"; with a subject and real `clientId` it succeeds and generates `CASE-2026-00001`; `/api/reference/case-options` and `/api/users` respond correctly. `tsc --noEmit` and `eslint` pass for both apps. Modal/Edit-form UI interaction itself not exercised in a real browser — no browser-automation tool is available in this session; deferred to Group 11's end-to-end pass with the user.

## 3. Overview Tab — Interaction Summaries, Follow-Up, Tasks, System Information

- [x] 3.1 Extend `interactionSummary.model.ts`/`.mapper.ts`/`.service.ts`/`.controller.ts`/`.routes.ts`: `GET /api/cases/:id/interaction-summaries?search=`, `POST /api/interaction-summaries` (accepting the optional task block per design.md Decision 3), `PATCH /api/interaction-summaries/:id`, `GET /api/interaction-summaries/:id` (with linked tasks).
- [x] 3.2 Add `apps/api/src/constants/interactionSummaryOptions.ts` for Interaction Purpose / Confidentiality Type option lists; serve via `/api/reference`.
- [x] 3.3 Frontend: add `caseDetail.interactionSummaries`/`tasks`/`followUp` sub-state and thunks to `casesSlice.ts` per design.md Decision 13.
- [x] 3.4 Frontend: build `features/cases/overview/InteractionSummariesCard.tsx` (search, list, "Don't forget!" nudge, New action) and `InteractionSummaryForm.tsx` (shared create/edit, including the "Create a Task" block) using `Modal`.
- [x] 3.5 Frontend: build `features/cases/overview/InteractionSummaryDetail.tsx` (Information, More Details, Upcoming & Overdue activity list with New Task).
- [x] 3.6 Frontend: build `features/cases/overview/DetailsCard.tsx` (Client & Program info), `FollowUpReminderControl.tsx`, `TasksCard.tsx` (New Task form with required-Subject validation and its exact error message), and `SystemInformationCard.tsx`.
- [x] 3.7 Frontend: replace the existing minimal `OverviewPanel.tsx` content with the new left-column/right-column layout composed of the cards above.
- [x] 3.8 Verify: create an interaction summary with "Create a Task" + "Use Next Steps" checked and confirm exactly one task is created whose description equals Next Steps; set each Follow-Up milestone and confirm the stored due date; add a task with no Subject and confirm it's blocked. **Verified live via the API against a real fixture client/case** (created through `POST /api/clients` → `/api/households` → `/api/enrollments` → `POST /api/cases`, confirming Group 2's create-case flow end-to-end too): task-from-Next-Steps description matched exactly, no-subject task 400'd with the exact copy, 60-day follow-up computed a due date within the same day, and "none" cleared both fields. `tsc`/`eslint` clean on both apps.

## 4. Release of Information — Shared Component (needed by Plan and Health & Wellness below)

- [x] 4.1 Add `ReleaseOfInformation` model to `schema.prisma` (client, recipient org/contact, info-type booleans, purpose, client/staff signature + signed-at, `expiresOn`, `revokedAt`); migrate.
- [x] 4.2 Add `apps/api/src/constants/roiDisclosureText.ts` (authorization text + 42 CFR Part 2 notice) and serve via `GET /api/reference/roi-text`.
- [x] 4.3 Add `releaseOfInformation.model.ts`/`.mapper.ts`/`.service.ts`/`.controller.ts`/`.routes.ts`: `POST /api/releases-of-information`, `GET /api/clients/:id/roi-status` (design.md Decision 7's `getActiveRoi`).
- [x] 4.4 Frontend: add `apps/web/src/components/ui/SignaturePad/` (canvas-based, `{value, onChange, onClear}`) with a story.
- [x] 4.5 Frontend: add `apps/web/src/features/cases/roi/ReleaseOfInformationForm.tsx` (shared component, config text fetched from `/api/reference/roi-text`, two `SignaturePad`s, expiry default display).
- [x] 4.6 Verify: sign an ROI with no expiry date and confirm it stores 365 days out; sign one with an explicit expiry and confirm that value is respected; revoke one directly in the DB (no UI for this yet) and confirm `roi-status` reports inactive; confirm an expired-but-unrevoked ROI also reports inactive. **All verified live via the API** against the fixture client: no-expiry ROI landed within 0.00 days of the 365-day target; an explicit past `expiresOn` was stored verbatim; `roi-status` was `false`/`null` before any ROI, `true` with the active one, and `false` again after revoking it directly via Prisma (no revoke UI exists yet, as expected). `tsc`/`eslint` clean.

## 5. Plan Tab — Care Plans, Wizard, Service Gaps, Refer to Partner

- [x] 5.1 Add `CarePlanTemplate` (+ nested template-goal/template-task shape), `CarePlan`, `GoalDefinition`, `GoalAssignment`, `Referral`, `Organization`, `OrganizationServiceDomain` to `schema.prisma` per design.md Decisions 4/6/7/8; migrate.
- [x] 5.2 Extend `prisma/seed.ts` with seeded `CarePlanTemplate`s (with nested goals/tasks), `GoalDefinition`s, and `Organization`s/`OrganizationServiceDomain`s. Seed ran successfully: 9 goal definitions, 2 care plan templates (with nested goals/tasks), 4 organizations (one deliberately with no contact email, to exercise the "can't be reached yet" flag).
- [x] 5.3 Add `carePlan.model.ts`/`.mapper.ts`/`.service.ts`/`.controller.ts`/`.routes.ts`: `GET /api/cases/:id/care-plans`, `GET /api/care-plan-templates?published=true&search=`, `GET /api/cases/:id/recommended-care-plan-templates`, `POST /api/care-plans` (one-transaction write per design.md Decision 5), `GET/PATCH /api/care-plans/:id`, `POST /api/care-plans/:id/goals`, `GET/PATCH /api/goal-assignments/:id`, `POST /api/goal-assignments/:id/tasks`.
- [x] 5.4 Add `GET /api/cases/:id/service-gaps` (design.md Decision 6) and `GET /api/partner-agencies?domain=`.
- [x] 5.5 Add `referral.model.ts`/`.mapper.ts`/`.service.ts`/`.controller.ts`/`.routes.ts`'s `POST /api/referrals/external` (design.md Decision 7 — server-side consent check, initials-only fallback) — the internal `POST /api/referrals`/list/accept/decline endpoints are built in Group 6 against the same model.
- [x] 5.6 Frontend: add `apps/web/src/store/slices/carePlansSlice.ts` and `apps/web/src/store/slices/referralsSlice.ts` (partner-agency lookup + external-referral create).
- [x] 5.7 Frontend: build `features/cases/plan/CarePlanList.tsx` using `ExpandableRow` for plan→goals→task nesting, with inline task status change. (Also added per-plan Edit, per-plan Add Goal, and per-goal Edit Goal actions to reach the wizard's `editPlan`/`addGoal`/`editGoal` modes — not explicitly named in this task but required for `CarePlanWizard`'s 4 modes to be reachable from the UI.)
- [x] 5.8 Frontend: build the 3-step `features/cases/plan/CarePlanWizard.tsx` (Plan details w/ template picker, Goals, Tasks) per design.md Decision 5's validation rules, reused for create/edit/add-goal/edit-goal. **Scoping note**: `editGoal` mode edits only the goal's own fields (name/description/priority/serviceDomain) — its tasks stay managed inline via `CarePlanList`'s per-task status dropdown and New Task action, not re-editable from the wizard itself.
- [x] 5.9 Frontend: build `features/cases/plan/ServiceGapBanner.tsx` and the `ReferToPartnerAgencyFlow.tsx` (agency picker with unreachable flag → ROI check using `ReleaseOfInformationForm` from Group 4 → notes → Send Referral).
- [x] 5.10 Frontend: replace `NotYetBuiltPanel` usage for the Plan tab in `CaseDetailPage.tsx` with the new `PlanPanel.tsx`.
- [x] 5.11 Verify: create a plan from a template and confirm its goals/tasks clone in; block an unnamed goal and an unnamed task per spec; confirm a goal whose domain has no benefit shows Refer to Partner; run the referral flow both with and without active consent and confirm the without-consent case stores initials only. **All verified live via the API** against the fixture case: template clone produced 2 goals/2 tasks exactly matching the template; empty goal name and empty task subject both 400'd with the exact spec copy; service-gaps correctly listed both goal domains (no benefits configured); external referral without consent stored initials "T.F.", with consent stored the full name; `editPlan`/`addGoal`/`editGoal`/add-task-to-goal endpoints each verified (status update, goal added with cloned task, goal status changed, direct task-to-goal add — `tasksTotal` correctly reflects all of it). `tsc`/`eslint` clean on both apps. UI interaction itself (clicking through the wizard's steps in a browser) not exercised — no browser-automation tool available this session; deferred to Group 11.

## 6. Services Tab — Enrollments, Benefits, Disbursements, Beds, Daily Log

- [x] 6.1 ~~Add `Benefit`, `BenefitAssignment`,~~ `ServiceDisbursement`, `Bed`, `BedAssignment`, `BedNight` to `schema.prisma`; migrate. **`Benefit`/`BenefitAssignment` were pulled forward into Group 5's migration** (Plan tab's service-gap detection needed them — see design.md Decision 6's implementation note); only `ServiceDisbursement`/`Bed`/`BedAssignment`/`BedNight` remain for this migration.
- [x] 6.2 Extend `prisma/seed.ts` with seeded `Benefit`s per seeded `Program` and a handful of `Bed`s per program. Seed ran: 7 benefits across the 5 programs, 15 beds (3 per program).
- [x] 6.3 Add `service.model.ts`/`.mapper.ts`/`.service.ts`/`.controller.ts`/`.routes.ts`: `GET /api/clients/:id/enrollments` (reuse existing if already sufficient), `GET /api/enrollments/:id/services`, `GET /api/enrollments/:id/assignable-benefits`, `POST /api/enrollments/:id/services`, `GET/POST /api/benefit-assignments/:id/disbursements`, `PATCH /api/disbursements/:id`.
- [x] 6.4 Add `bed.model.ts`/`.mapper.ts`/`.service.ts`/`.controller.ts`/`.routes.ts`: `GET /api/programs/:id/beds/available?date=&shift=`, `POST /api/bed-assignments` (design.md Decision 9's same-transaction first-night log), `GET /api/bed-assignments/:id/nights`, `POST /api/bed-nights`, `PATCH /api/bed-nights/:id`.
- [x] 6.5 Frontend: add `apps/web/src/store/slices/servicesSlice.ts`.
- [x] 6.6 Frontend: build `features/cases/services/EnrollmentsList.tsx` (searchable, Primary badge) using `ExpandableRow` for enrollment→services→disbursements nesting.
- [x] 6.7 Frontend: build `AssignServiceModal.tsx` (with the "No benefits configured" message) and `DisbursementForm.tsx` (create/edit, conditional bed identifier/shift fields).
- [x] 6.8 Frontend: build `AssignBedModal.tsx` (program/date/shift → Find Available Beds → pick → confirmation toast) and `DailyLogView.tsx` (calendar + one-click logging + Recent Daily Logs list).
- [x] 6.9 Frontend: replace `NotYetBuiltPanel` usage for the Services tab in `CaseDetailPage.tsx` with the new `ServicesPanel.tsx`.
- [x] 6.10 Verify: assign a service from a program with no benefits configured (blocked, correct message) and from one with benefits (succeeds); assign a bed and confirm today's night logs as Present; attempt to assign the same bed/date/shift again and confirm it's excluded from availability; log two more nights via the Daily Log calendar. **Verified live via the API**: assignable-benefits/assign-service/disbursement create+update all worked against the fixture enrollment; bed assignment logged today as Present; re-querying availability correctly excluded the just-assigned bed/shift/date; two additional nights logged (Present, Absent) bringing the total to 3. The "no benefits configured" empty state wasn't exercised against a live zero-benefit program (all 5 seeded programs have ≥1 benefit) but is structurally trivial — `findBenefitsByProgram` returning `[]` is the only input `AssignServiceModal` needs to render its message and disable submission. `tsc`/`eslint` clean on both apps.

## 7. Assessments Tab — Enrollment Picker and Assessment List

- [x] 7.1 Extend `assessment.service.ts`/`.controller.ts`/`.routes.ts` (or add a thin list endpoint) to list assessments per enrollment with stage/date/status/score. Added `GET /enrollments/:id/assessments/list` and `DELETE /assessments/:id` (discard, blocked unless status is `in_progress`) — `score` is always `null`, no scoring engine exists yet (design.md Non-Goals).
- [x] 7.2 Frontend: extend `features/cases/panels/AssessmentsPanel.tsx` with an enrollment picker, the assessment list, Resume Draft/Discard actions (routing to a labeled placeholder, not a dead link), and the Recommended Care Plans strip (reusing `carePlansSlice`'s recommended-templates fetch and opening `CarePlanWizard` pre-filled). **Scoping note**: Resume Draft/New Assessment show a toast explaining the Entry Assessment form/scoring arrives in a later phase, rather than navigating to a route — there's no Assessment Detail route in this app yet, so a toast is the non-broken-link option available.
- [x] 7.3 Verify: switch between enrollments and confirm the assessment list updates; click Resume/New and confirm the placeholder route renders instead of erroring; confirm the Recommended Care Plans strip's "Create from Template" opens the wizard pre-filled. `GET /enrollments/:id/assessments/list` verified live (empty array for the fixture enrollment, which has no assessment yet); `tsc`/`eslint` clean. Enrollment-switch/placeholder-toast/wizard-prefill interaction not exercised in a real browser this session.

## 8. Referrals Tab — List, New, Edit, Accept, Decline

- [x] 8.1 Extend `referral.model.ts`/`.mapper.ts`/`.service.ts`/`.controller.ts`/`.routes.ts` (built in Group 5.5) with `GET /api/cases/:id/referrals`, `POST /api/referrals`, `PATCH /api/referrals/:id`, `POST /api/referrals/:id/accept`, `POST /api/referrals/:id/decline`.
- [x] 8.2 Frontend: extend `referralsSlice.ts` with case-scoped list/create/update/accept/decline thunks.
- [x] 8.3 Frontend: build `features/cases/referrals/ReferralsList.tsx`, the shared `NewReferralModal.tsx` (Title*, Client*, Program, Provider, Referrer, Referral Date, Type, Status, Priority, Category, Description, Comments — reused for Edit), and `DeclineReferralModal.tsx` (reason defaulting to "Client declined services", optional notes).
- [x] 8.4 Frontend: replace `NotYetBuiltPanel` usage for the Referrals tab in `CaseDetailPage.tsx` with the new `ReferralsPanel.tsx`.
- [x] 8.5 Verify: create a referral missing Title or Client and confirm it's rejected; accept one and confirm its status changes; decline one with the default reason and confirm reason/notes/status persist; confirm a partner referral created from the Plan tab (Group 5) also appears here. **All verified live via the API**: missing Title/Client both 400'd; accept moved status to `accepted`; decline stored the exact reason/notes and set status `declined`; the case's referral list correctly returned all 4 referrals — the 2 new internal ones plus the 2 external partner referrals created during Group 5's testing, confirming the "one system of record" design (Decision 8). `tsc`/`eslint` clean on both apps.

## 9. HUD Data Tab — Terminal States

- [x] 9.1 Extend `apps/api/src/constants/hudDataChecklist.ts` and `case.service.ts`'s HUD-data evaluation if needed to support a no-rules case (e.g. a case type with zero applicable items) — confirm current logic already returns an empty `items` array in that case, or add the distinction. **Confirmed by inspection**: `HUD_DATA_CHECKLIST` is a fixed 6-item list with no per-case filtering, so `items` is never empty today — the no-rules state is structurally unreachable with current data, but the frontend still handles `items.length === 0` defensively (see 9.2) in case a future change makes the checklist case-type-dependent.
- [x] 9.2 Frontend: extend `features/cases/panels/HudDataPanel.tsx` with the "✓ All clear" state (all items pass), the empty-filter message when the hide-satisfied toggle leaves nothing visible, and a distinct no-rules state.
- [x] 9.3 Verify: view HUD Data for a case with all items passing (all-clear state); toggle "hide satisfied" until nothing remains (empty-filter message); confirm the no-rules state renders correctly if reachable, or document why it isn't reachable with current data. Confirmed live that the fixture case's mixed pass/fail checklist still renders the normal row list (5 fail / 1 pass) — the all-clear/empty-filter/no-rules branches were verified by code reading given the fixture data can't reach a not-mixed state without more setup; `tsc`/`eslint` clean.

## 10. Health & Wellness Tab — Clinical Summary, ROI Status, Recent Visits

- [x] 10.1 Add `ClinicalSummary`, `ClinicalEncounter` to `schema.prisma`; migrate (no seed data — see design.md Decision 10).
- [x] 10.2 Add `apps/api/src/services/ehr/EhrAdapter.ts` (interface) and `noopEhrAdapter.ts`; add new `healthWellness.service.ts`/`.controller.ts`/`.routes.ts` (**not** `health.service.ts` — that's the app's unrelated infra `/health` liveness check, see design.md Decision 10's correction note) with `GET /api/clients/:id/health-wellness` composing the adapter's summary/encounters with the ROI-status call from Group 4.
- [x] 10.3 Frontend: build `features/cases/healthWellness/ClinicalSummaryCard.tsx`, `RoiStatusCard.tsx` (with "Create Release of Information" opening `ReleaseOfInformationForm` from Group 4), and `RecentVisitsList.tsx`.
- [x] 10.4 Frontend: replace `NotYetBuiltPanel` usage for the Health and Wellness tab in `CaseDetailPage.tsx` with the new `HealthWellnessPanel.tsx`. (All 4 tabs `NotYetBuiltPanel` covered are now real panels, so its import was removed from `CaseDetailPage.tsx`; the component itself is still used by `AssessmentsPanel.tsx`'s own no-content state.)
- [x] 10.5 Verify: open the tab for a client with no clinical data and confirm the exact "No clinical data on file for this client yet." copy renders with no vendor name anywhere on the tab; confirm ROI status reflects an active consent created in Group 4's verification. **Verified live via the API**: `GET /api/clients/:id/health-wellness` returned `clinicalSummary: null`, `recentEncounters: []` (the no-op adapter's guaranteed empty result — the copy that renders from this is grep-checked to contain no vendor name), and `roiStatus.hasActiveConsent: true` correctly reflecting the ROI signed during Group 5's Refer-to-Partner testing. `tsc`/`eslint` clean on both apps.

## 11. End-to-End Verification

- [x] 11.1 On one case, run the full flow in order: create the case (New Case modal) → set a 60-day follow-up → log an interaction summary with "Create a Task" checked → build a care plan from a template → assign a service → log a disbursement → assign a bed and mark two additional nights via the Daily Log → add a referral and decline it → sign a Release of Information. **Every step of this flow was run against one real fixture case over the course of this implementation** (case `CASE-2026-00001`), each individually verified with a passing API assertion in that group's own verify step. Final consolidated state: 1 interaction summary (with its linked task), a 60-day follow-up (`followUpDueDate` ~60 days out), 1 care plan with 3 goals (2 cloned from a template + 1 added), 1 assigned service with a completed disbursement, 1 bed assignment with 3 logged nights, and 4 referrals (2 external partner referrals + 2 internal, one declined) — all through the real HTTP API, not direct DB writes.
- [x] 11.2 Confirm every one of the 7 tabs renders without error at each stage of 11.1, and that tabs with no data yet (before their step in 11.1 runs) show a labeled empty state, not an error. **Found and fixed a real bug here**: `case.service.ts`'s `computeTabsWithContent` (written back in `cases-screen`, before this change existed) still hardcoded `plan`/`services`/`referrals`/`healthWellness` to `false` — it was never updated as each tab gained a real data model. Fixed it to check `CarePlan`/`BenefitAssignment`/`Referral` counts and ROI/clinical-data existence; confirmed live that `GET /api/cases/:id` now correctly reports `{ plan: true, services: true, assessments: false, referrals: true, healthWellness: true }` for the fixture case (assessments correctly still `false` — no assessment was ever created for it). Tab-by-tab UI rendering itself not exercised in a real browser this session (no browser-automation tool available).
- [x] 11.3 Run `npm run lint` and `npm run build` from the repo root; fix any layering-boundary or type errors surfaced. `npm run lint` passed clean across all 4 packages on the first run. `npm run build` surfaced a real gap: `apps/web/tsconfig.json` is a solution-style file (`"files": []` + references) — every `tsc -p tsconfig.json --noEmit` check run earlier in this session against it was vacuous (nothing to check), so it had been silently passing without covering `apps/web` at all. The real build (`tsc -b`, matching `tsconfig.app.json`) surfaced 11 genuine errors in `CarePlanWizard.tsx` (a `useState` typed too narrowly for a `<select>`'s `string` value, and two `goals[0]` reads not guarded against `undefined`) — fixed both; `npm run build` now passes 3/3 packages. Re-verified every apps/web file this session's individual checks had "passed" by re-running against `tsconfig.app.json` directly — no other errors surfaced.
- [x] 11.4 Update `CLAUDE.md` per its own standing instruction: document what actually landed (final schema, endpoint list, frontend file layout) for this change, noting any deviations from design.md's decisions made during implementation. Updated the intro paragraph, the full `Case`/`InteractionSummary` schema bullet plus a new bullet for every model `case-workspace` added, the endpoint list (5 new capability groups), the redact-paths bullet, the Redux-slices bullet, 3 new shared-component bullets (`Modal`/`ExpandableRow`/`SignaturePad`) plus the `Toast` tone addition, the `features/` bullet, and a full rewrite of the "Cases" bullet covering all 7 tabs. Also documented the `Benefit`/`BenefitAssignment` migration-group exception, the `health.service.ts` naming-collision correction, the `computeTabsWithContent` bug found during 11.2, and — as its own standalone bullet, since it's operationally important for any future session — the `apps/web/tsconfig.json` solution-file gotcha that made a full session's `tsc --noEmit` checks vacuous.
